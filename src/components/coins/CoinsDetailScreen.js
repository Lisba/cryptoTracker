import React from 'react';
import { View, Text } from 'react-native';

const CoinsDetailScreen = () => {
  return (
    <View>
      <Text>Coins Detail Screen</Text>
    </View>
  );
};

export default CoinsDetailScreen;
